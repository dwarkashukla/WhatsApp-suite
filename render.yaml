services:
  - type: web
    name: whatsapp-suite-backend
    env: node
    plan: free

    buildCommand: |
      cd backend
      npm install
      cd ../frontend
      npm install
      npm run build

    startCommand: |
      cd backend
      node server.js

    envVars:
      - key: NODE_ENV
        value: production

      - key: PORT
        value: 10000

      - key: CLIENT_URL
        sync: false

      - key: MONGO_URI
        sync: false

      - key: JWT_SECRET
        generateValue: true

      - key: JWT_REFRESH_SECRET
        generateValue: true
